---
const html = await Astro.slots.render("default");

const regex = /<([a-z][a-z0-9]*)\b[^>]*>(.*?)<\/\1>/g;
const match = html.match(regex);
---

<code class:list={[!match && "rounded p-1 bg-gray-400/10"]}
  ><Fragment set:html={html} /></code
>

---
handle: zod-error-utils
name: zod-error-utils
owner: luxass
description: Zod error utilities
githubUrl: https://github.com/luxass/zod-error-utils
npm: "zod-error-utils"
---

A set of utilities to work with Zod errors.

## 📦 Installation

```bash
npm install zod-error-utils
```

## 🚀 Usage

```ts
import { buildZodErrorMap, flattenErrorPath, zodErrorMap } from "zod-error-utils";

const schema = z.object({
  user: z.object({
    name: z.string(),
    isAdministrator: z.boolean(),
  }),
});

const data = {
  user: {
    name: "John Doe",
    isAdministrator: true,
  },
};

const result = schema.safeParse(data, {
  errorMap: zodErrorMap
});

// you can customize the zodErrorMap, by creating a new one using `buildZodErrorMap`.

const customErrorMap = buildZodErrorMap({
  prefixFn(path, message) {
    return `Custom prefix: ${path.join(".")}: ${message}`;
  },
});
```

## 📄 License

Published under [MIT License](https://github.com/luxass/zod-error-utils/blob/main/LICENSE).

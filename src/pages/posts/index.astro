---
import { getCollection } from "astro:content";
import PostCard from "../../components/cards/PostCard.astro";
import BaseLayout from "../../layouts/BaseLayout.astro";

const posts = await getCollection("posts", ({ data }) => !data.draft).then((entries) =>
  entries.toSorted((a, b) => b.data.date.getTime() - a.data.date.getTime()),
);

export const prerender = true;
---

<BaseLayout title="posts | luxass.dev" description="writing about web development, programming, and more">
  <h1 class="text-xl font-bold mb-4 text-foreground">Posts</h1>

  <p class="text-foreground mb-12 leading-relaxed text-sm">here is some of my posts i have written.</p>

  <div class="space-y-12">
    {
      posts.length > 0 ? (
        posts.map((post) => <PostCard post={post} />)
      ) : (
        <p class="text-foreground">
          No posts yet. In the meantime, you can find my work on GitHub at
          <a class="underline" href="https://github.com/luxass">
            github.com/luxass
          </a>
          .
        </p>
      )
    }
  </div>
</BaseLayout>

---
import PageLayout from "../../layouts/PageLayout.astro";

export const prerender = true;
---

<PageLayout title="projects | luxass.dev" description="projects">
  <article class="prose">
    <h1>Projects</h1>
    <p>This is some of my projects.</p>
    <!-- {
      allProjects.length > 0 ? (
        <div class="grid auto-cols-max grid-cols-1 mt-4 gap-4 sm:grid-cols-2 sm:gap-3" id="search-results">
          {allProjects.map(({ data: project }) => (
            <ProjectPreviewCard {project} />
          ))}
        </div>
      ) : (
        <p>
          I haven't made any of my projects available here yet - you can always checkout my{" "}
          <a href="https://github.com/luxass" target="_blank" rel="noopener noreferrer">
            GitHub
          </a>
        </p>
      )
    } -->
  </article>
</PageLayout>

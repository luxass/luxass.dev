{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "update:schema": "curl -H \"Authorization: bearer $GITHUB_TOKEN\" https://api.github.com/graphql -o github-graphql.schema.json"
  },
  "devDependencies": {
    "@iconify-json/carbon": "^1.1.16",
    "@unocss/nuxt": "^0.51.8",
    "@unocss/preset-icons": "^0.51.8",
    "nuxt": "^3.4.3"
  },
  "dependencies": {
    "nuxt-graphql-client": "^0.2.27"
  }
}
